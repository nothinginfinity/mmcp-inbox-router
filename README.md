# mmcp-inbox-router
GitHub-native MMCP message router — reads inbox ledgers, classifies envelopes deterministically, and dispatches actions (issues, threads, status, CI triggers). The Phase 1 engine core of pocket-agent-engine.
